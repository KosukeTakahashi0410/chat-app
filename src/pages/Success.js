const Success = () => {
  // 閉じるボタンは実装できなさそうなので却下
  // TODO　Takahashi 戻るのときにフォームへ遷移できないようにする、多分routerの機能であるはず
  return (
    <div>
      <p>Your Signup almost done.</p>
      <p>Check your email and confirm your acount!</p>
    </div>
  );
}

export default Success